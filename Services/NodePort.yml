apiVersion: v1
kind: Service
metadata:
  labels:
    app: deploy1
  name: deploy1
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30080
  selector:
    app: deploy1
  type: NodePort
status:
  loadBalancer: {}
