apiVersion: v1
kind: Service
metadata:
  labels:
    app: deploy1
  name: deploy1
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: deploy1
  type: ClusterIP
status:
  loadBalancer: {}
